# Runtime mode
#   development -> local hot reload via `make dev`
#   production  -> VPS / public hosting via `make prod`
APP_ENV=development

# Demo mode toggle
#   false -> show live/mock switch in the UI
#   true  -> force mock responses for demo deployments (no API costs)
VITE_FORCE_MOCK_MODE=false

# Backend configuration (Docker containers reference the `db` hostname)
DATABASE_URL=postgresql+psycopg://app:app@db:5432/hidden_messages
LOG_LEVEL=debug

# CORS origin whitelist (applies to both dev and prod stacks)
# Include every origin that should access the API
ALLOWED_ORIGINS=http://localhost,http://localhost:5173

# API base URL override (defaults to backend container when blank)
VITE_API_BASE_URL=http://localhost:8000

# API keys for LLM providers (required when running live sessions)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# Game configuration
TRIES_TOTAL=3