services:
  web:
    ports:
      - "5173:5173"
    command: >
      /bin/sh -c "npm run dev -- --host 0.0.0.0 --port 5173"

  api:
    ports:
      - "8000:8000"
    command: >
      /bin/sh -c "uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --log-level ${LOG_LEVEL:-debug}"
